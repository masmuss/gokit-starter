pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.go"
      run: gofmt -w {staged_files} && goimports -w {staged_files}
      stage_fixed: true

    linter:
      glob: "*.go"
      run: golangci-lint run ./...

    tidy:
      run: go mod tidy

pre-push:
  parallel: true
  commands:
    test:
      glob: "*.go"
      run: go test ./...

    build:
      glob: "*.go"
      run: go build ./...

    vet:
      glob: "*.go"
      run: go vet ./...
